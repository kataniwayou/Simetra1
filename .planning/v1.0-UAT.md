---
status: completed
phase: project-level
source: codebase analysis (cross-cutting concerns)
started: 2026-02-16T08:00:00Z
updated: 2026-02-16T11:20:00Z
---

## Tests

### 1. State Vector key uniqueness within a device
expected: Two polls on same device produce different keys (format: "{deviceName}:{metricName}")
result: pass

### 2. State Vector key uniqueness between devices
expected: Same MetricName on different devices produces different keys (e.g., "npb-01:port_rx_octets" vs "npb-02:port_rx_octets")
result: pass

### 3. Liveness Vector key uniqueness within a device (OID jobs)
expected: Two poll jobs on same device produce different keys (format: "state-poll-{deviceName}-{metricName}" or "metric-poll-{deviceName}-{metricName}")
result: pass

### 4. Liveness Vector key uniqueness between devices (OID jobs)
expected: Same poll on different devices produces different keys (e.g., "state-poll-npb-01-port_rx_packets" vs "state-poll-npb-02-port_rx_packets")
result: pass

### 5. Liveness Vector key uniqueness (internal jobs)
expected: Heartbeat and Correlation jobs use hardcoded unique keys ("heartbeat", "correlation")
result: pass

### 6. Liveness Vector key mismatch (Quartz ToString vs IntervalRegistry)
expected: Job stamps and IntervalRegistry use matching key formats so LivenessHealthCheck can detect stale jobs
result: pass (fixed)
fix: Changed `context.JobDetail.Key.ToString()` to `context.JobDetail.Key.Name` in all 4 job files (CorrelationJob, HeartbeatJob, MetricPollJob, StatePollJob). Quartz `ToString()` returns "DEFAULT.{name}" but IntervalRegistry uses plain "{name}".

### 7. Config validation: duplicate IP detection
expected: DevicesOptionsValidator rejects configuration with two devices sharing the same IpAddress
result: pass (fixed)
fix: Added `ValidateNoDuplicates()` method to DevicesOptionsValidator that detects duplicate IPs across all devices.

### 8. Config validation: duplicate device name detection
expected: DevicesOptionsValidator rejects configuration with two devices sharing the same Name
result: pass (fixed)
fix: Added duplicate name detection (case-insensitive) in same `ValidateNoDuplicates()` method.

### 9. Config validation: DeviceType whitelist includes NPB and OBP
expected: KnownDeviceTypes includes "NPB" and "OBP" so appsettings.json passes ValidateOnStart
result: pass (fixed)
fix: Added "npb" and "obp" to KnownDeviceTypes HashSet in DevicesOptionsValidator.

## Summary

total: 9
passed: 9
issues: 0 (4 issues found and fixed)
pending: 0
skipped: 0
