---
phase: 12-npb-device-module
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/Simetra/Extensions/ServiceCollectionExtensions.cs
  - src/Simetra/appsettings.json
autonomous: true

must_haves:
  truths:
    - "NpbModule is registered as IDeviceModule singleton in AddDeviceModules()"
    - "NpbModule is included in allModules array for Quartz state poll job registration"
    - "NPB device entry exists in appsettings.json with Configuration-source polls for RxOctets and TxOctets"
    - "All existing tests still pass with the new registrations"
  artifacts:
    - path: "src/Simetra/Extensions/ServiceCollectionExtensions.cs"
      provides: "NpbModule DI registration and Quartz job registration"
      contains: "NpbModule"
    - path: "src/Simetra/appsettings.json"
      provides: "NPB device configuration with metric polls"
      contains: "npb-2e-01"
  key_links:
    - from: "src/Simetra/Extensions/ServiceCollectionExtensions.cs"
      to: "NpbModule"
      via: "AddSingleton<IDeviceModule, NpbModule>()"
      pattern: "AddSingleton.*NpbModule"
    - from: "src/Simetra/Extensions/ServiceCollectionExtensions.cs"
      to: "allModules array"
      via: "new NpbModule() in Quartz registration"
      pattern: "new NpbModule\\(\\)"
    - from: "src/Simetra/appsettings.json"
      to: "NPB device"
      via: "Devices array entry"
      pattern: "npb-2e-01"
---

<objective>
Register NpbModule in DI, Quartz job scheduling, and add NPB device configuration to appsettings.json with Configuration-source poll definitions.

Purpose: NPB-09 requires the module to be registered in the device module registry, NPB-04 and NPB-05 require Configuration-source poll definitions in appsettings.json for RxOctets and TxOctets. These are the three registration touchpoints that connect NpbModule to the running pipeline.

Output: Updated ServiceCollectionExtensions.cs with NpbModule registration, updated appsettings.json with NPB device entry.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-npb-device-module/12-RESEARCH.md
@.planning/phases/12-npb-device-module/12-01-SUMMARY.md
@src/Simetra/Extensions/ServiceCollectionExtensions.cs
@src/Simetra/appsettings.json
@src/Simetra/Devices/NpbModule.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register NpbModule in DI and Quartz job scheduling</name>
  <files>src/Simetra/Extensions/ServiceCollectionExtensions.cs</files>
  <action>
Make two targeted edits to `src/Simetra/Extensions/ServiceCollectionExtensions.cs`:

**Edit 1: AddDeviceModules() method (~line 289)**

Add NpbModule registration after SimetraModule. Replace the existing future module comments:

```csharp
public static IServiceCollection AddDeviceModules(this IServiceCollection services)
{
    services.AddSingleton<IDeviceModule, SimetraModule>();
    services.AddSingleton<IDeviceModule, NpbModule>();

    return services;
}
```

Remove the commented-out future module lines (`// services.AddSingleton<IDeviceModule, RouterModule>();` etc.) since we now have a real second module.

Add the `using Simetra.Devices;` import if not already present (it should be, since SimetraModule is already referenced).

**Edit 2: allModules array in AddScheduling() (~line 425-426)**

Update the module instantiation array to include NpbModule:

```csharp
var simetraModule = new SimetraModule();
var npbModule = new NpbModule();
var allModules = new IDeviceModule[] { simetraModule, npbModule };
```

This ensures NpbModule.StatePollDefinitions (RxPackets, TxPackets, LinkStatus) get registered as Quartz StatePollJob instances with their IntervalSeconds triggers.

Do NOT modify any other code in this file.
  </action>
  <verify>
Build succeeds: `dotnet build src/Simetra/Simetra.csproj`
Grep confirms registrations: `NpbModule` appears in both AddDeviceModules and allModules
  </verify>
  <done>
NpbModule registered as IDeviceModule singleton in AddDeviceModules(). NpbModule instantiated and included in allModules array for Quartz state poll job scheduling. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add NPB device configuration to appsettings.json</name>
  <files>src/Simetra/appsettings.json</files>
  <action>
Add an NPB device entry to the `Devices` array in `src/Simetra/appsettings.json`. This entry provides:
- Device identity (matches NpbModule.DeviceName and NpbModule.IpAddress)
- Configuration-source poll definitions for NPB-04 (RxOctets) and NPB-05 (TxOctets)

Add the following entry to the Devices array (after the existing "switch-floor-2" entry):

```json
{
  "Name": "npb-2e-01",
  "IpAddress": "10.0.10.1",
  "DeviceType": "NPB",
  "MetricPolls": [
    {
      "MetricName": "port_rx_octets",
      "MetricType": "Counter",
      "Oids": [
        {
          "Oid": "1.3.6.1.4.1.47477.100.4.2.2.5.1.1.3",
          "PropertyName": "port_rx_octets",
          "Role": "Metric"
        }
      ],
      "IntervalSeconds": 30
    },
    {
      "MetricName": "port_tx_octets",
      "MetricType": "Counter",
      "Oids": [
        {
          "Oid": "1.3.6.1.4.1.47477.100.4.2.2.5.1.1.4",
          "PropertyName": "port_tx_octets",
          "Role": "Metric"
        }
      ],
      "IntervalSeconds": 30
    }
  ]
}
```

Key details:
- Name and IpAddress MUST match NpbModule.DeviceName ("npb-2e-01") and NpbModule.IpAddress ("10.0.10.1") exactly
- DeviceType MUST match NpbModule.DeviceType ("NPB")
- MetricType is "Counter" (Counter64 SNMP type maps to Counter metric type)
- OIDs are from portStatisticsSummaryPortEntry: .3 = RxOctets, .4 = TxOctets
- IntervalSeconds: 30 (matches existing router-core-1 poll intervals)
- These are Configuration-source polls — the Source field is automatically stamped by PostConfigure as "Configuration" since they come from appsettings.json

Ensure valid JSON (proper comma after the preceding device entry).
  </action>
  <verify>
JSON validates: `dotnet build src/Simetra/Simetra.csproj` (invalid JSON would fail config binding)
All tests pass: `dotnet test tests/Simetra.Tests/Simetra.Tests.csproj`
  </verify>
  <done>
NPB device entry exists in appsettings.json Devices array with Name "npb-2e-01", IpAddress "10.0.10.1", DeviceType "NPB", and 2 Configuration-source MetricPolls (port_rx_octets on OID ...5.1.1.3, port_tx_octets on OID ...5.1.1.4). All tests pass.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/Simetra/Simetra.csproj` -- compiles without errors
2. `dotnet test tests/Simetra.Tests/Simetra.Tests.csproj` -- all tests pass (no regressions from new registrations/config)
3. ServiceCollectionExtensions.cs contains `AddSingleton<IDeviceModule, NpbModule>()`
4. ServiceCollectionExtensions.cs contains `new NpbModule()` in allModules array
5. appsettings.json contains "npb-2e-01" device with 2 MetricPolls
6. appsettings.json remains valid JSON
</verification>

<success_criteria>
- NpbModule registered in all 3 touchpoints (DI, Quartz allModules, appsettings.json)
- Configuration-source polls (RxOctets, TxOctets) defined in appsettings.json with correct OIDs
- All existing tests pass -- no regressions from registration changes
- NPB-04, NPB-05, NPB-09 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/12-npb-device-module/12-02-SUMMARY.md`
</output>
