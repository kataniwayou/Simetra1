---
phase: 12-npb-device-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Simetra/Devices/NpbModule.cs
  - tests/Simetra.Tests/Devices/NpbModuleTests.cs
autonomous: true

must_haves:
  truths:
    - "NpbModule implements IDeviceModule with DeviceType 'NPB'"
    - "TrapDefinitions contains portLinkUp with 6 varbind OIDs and portLinkDown with 5 varbind OIDs"
    - "StatePollDefinitions contains RxPackets, TxPackets, and LinkStatus polls with Source=Module"
    - "LinkStatusType EnumMap maps {-1:unknown, 0:down, 1:up, 2:receiveDown, 3:forcedDown}"
    - "All OIDs are correctly derived from NPB MIB hierarchy with base prefix 1.3.6.1.4.1.47477.100.4"
  artifacts:
    - path: "src/Simetra/Devices/NpbModule.cs"
      provides: "NpbModule sealed class implementing IDeviceModule"
      contains: "sealed class NpbModule : IDeviceModule"
    - path: "tests/Simetra.Tests/Devices/NpbModuleTests.cs"
      provides: "Unit tests verifying NpbModule definitions"
      contains: "class NpbModuleTests"
  key_links:
    - from: "src/Simetra/Devices/NpbModule.cs"
      to: "IDeviceModule"
      via: "interface implementation"
      pattern: "class NpbModule : IDeviceModule"
    - from: "src/Simetra/Devices/NpbModule.cs"
      to: "PollDefinitionDto"
      via: "TrapDefinitions and StatePollDefinitions properties"
      pattern: "IReadOnlyList<PollDefinitionDto>"
---

<objective>
Create NpbModule implementing IDeviceModule with all trap definitions, state poll definitions, and EnumMap for the NPB device.

Purpose: NPB-01 through NPB-08 require the NpbModule class to define the device identity, two trap definitions (portLinkUp, portLinkDown), two module-source state polls (RxPackets, TxPackets), and one EnumMap-bearing state poll (LinkStatus). This is the core domain logic for the NPB reference device module.

Output: NpbModule.cs with complete definitions, NpbModuleTests.cs with comprehensive unit tests.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-npb-device-module/12-RESEARCH.md
@src/Simetra/Devices/IDeviceModule.cs
@src/Simetra/Devices/SimetraModule.cs
@src/Simetra/Models/PollDefinitionDto.cs
@src/Simetra/Models/OidEntryDto.cs
@src/Simetra/Configuration/MetricType.cs
@src/Simetra/Configuration/MetricPollSource.cs
@src/Simetra/Configuration/OidRole.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NpbModule implementing IDeviceModule</name>
  <files>src/Simetra/Devices/NpbModule.cs</files>
  <action>
Create `src/Simetra/Devices/NpbModule.cs` as a sealed class implementing IDeviceModule. Follow the SimetraModule pattern exactly.

**OID Constants (all derived from MIB hierarchy, verified in 12-RESEARCH.md):**

Base prefix: `1.3.6.1.4.1.47477.100.4` (enterprises > cgs(47477) > npb(100) > npb-2e(4))

Define as `public const string` or `private const string` fields on the class:
- `Npb2ePrefix = "1.3.6.1.4.1.47477.100.4"` (private)
- Trap notification OIDs (public, needed for trap matching):
  - `PortLinkUpTrapOid = Npb2ePrefix + ".10.2.101"`
  - `PortLinkDownTrapOid = Npb2ePrefix + ".10.2.102"`
- Trap varbind OIDs (private):
  - `ModuleOid = Npb2ePrefix + ".10.1.1"` (variables.1)
  - `SeverityOid = Npb2ePrefix + ".10.1.2"` (variables.2)
  - `TypeOid = Npb2ePrefix + ".10.1.3"` (variables.3)
  - `MessageOid = Npb2ePrefix + ".10.1.4"` (variables.4)
- Port entry OIDs (private):
  - `PortEntryPrefix = Npb2ePrefix + ".2.1.4.1"`
  - `PortLogicalPortNumberOid = PortEntryPrefix + ".1"` (portsPortEntry.1)
  - `PortSpeedOid = PortEntryPrefix + ".4"` (portsPortEntry.4)
  - `PortLinkStatusOid = PortEntryPrefix + ".3"` (portsPortEntry.3)
- Port statistics summary OIDs (private):
  - `SummaryEntryPrefix = Npb2ePrefix + ".2.2.5.1.1"`
  - `RxPacketsOid = SummaryEntryPrefix + ".5"` (portStatisticsSummaryPortEntry.5)
  - `TxPacketsOid = SummaryEntryPrefix + ".6"` (portStatisticsSummaryPortEntry.6)

**EnumMap (private static readonly):**
```csharp
private static readonly IReadOnlyDictionary<int, string> LinkStatusEnumMap =
    new Dictionary<int, string>
    {
        { -1, "unknown" },
        {  0, "down" },
        {  1, "up" },
        {  2, "receiveDown" },
        {  3, "forcedDown" }
    }.AsReadOnly();
```

**IDeviceModule properties:**
- `DeviceType => "NPB"`
- `DeviceName => "npb-2e-01"`
- `IpAddress => "10.0.10.1"`

**TrapDefinitions (IReadOnlyList&lt;PollDefinitionDto&gt;):**

1. portLinkUp trap (NPB-02):
   - MetricName: `"port_link_up"`
   - MetricType: `MetricType.Gauge`
   - Source: `MetricPollSource.Module`
   - IntervalSeconds: `0` (traps are event-driven, not polled)
   - 6 OidEntryDto varbinds:
     - (ModuleOid, "module", OidRole.Label, null)
     - (SeverityOid, "severity", OidRole.Label, null)
     - (TypeOid, "type", OidRole.Label, null)
     - (MessageOid, "message", OidRole.Label, null)
     - (PortLogicalPortNumberOid, "port_number", OidRole.Label, null)
     - (PortSpeedOid, "port_speed", OidRole.Metric, null)

2. portLinkDown trap (NPB-03):
   - MetricName: `"port_link_down"`
   - MetricType: `MetricType.Gauge`
   - Source: `MetricPollSource.Module`
   - IntervalSeconds: `0`
   - 5 OidEntryDto varbinds:
     - (ModuleOid, "module", OidRole.Label, null)
     - (SeverityOid, "severity", OidRole.Label, null)
     - (TypeOid, "type", OidRole.Label, null)
     - (MessageOid, "message", OidRole.Label, null)
     - (PortLogicalPortNumberOid, "port_number", OidRole.Label, null)

**StatePollDefinitions (IReadOnlyList&lt;PollDefinitionDto&gt;):**

1. RxPackets poll (NPB-06):
   - MetricName: `"port_rx_packets"`
   - MetricType: `MetricType.Counter`
   - Source: `MetricPollSource.Module`
   - IntervalSeconds: `30`
   - 1 OidEntryDto: (RxPacketsOid, "port_rx_packets", OidRole.Metric, null)

2. TxPackets poll (NPB-07):
   - MetricName: `"port_tx_packets"`
   - MetricType: `MetricType.Counter`
   - Source: `MetricPollSource.Module`
   - IntervalSeconds: `30`
   - 1 OidEntryDto: (TxPacketsOid, "port_tx_packets", OidRole.Metric, null)

3. LinkStatus poll (NPB-08):
   - MetricName: `"port_link_status"`
   - MetricType: `MetricType.Gauge`
   - Source: `MetricPollSource.Module`
   - IntervalSeconds: `30`
   - 1 OidEntryDto: (PortLinkStatusOid, "port_link_status", OidRole.Metric, LinkStatusEnumMap)

Add XML doc comments on the class similar to SimetraModule, explaining this is the NPB-2E reference device module for a standard SNMP device with NOTIFICATION-TYPE traps and table-based port statistics.
  </action>
  <verify>
Build succeeds: `dotnet build src/Simetra/Simetra.csproj`
NpbModule can be instantiated: verify no constructor errors
  </verify>
  <done>
NpbModule.cs exists with DeviceType "NPB", 2 trap definitions (portLinkUp with 6 varbinds, portLinkDown with 5 varbinds), 3 state poll definitions (RxPackets, TxPackets, LinkStatus with EnumMap), all OIDs matching the MIB hierarchy.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create NpbModuleTests unit tests</name>
  <files>tests/Simetra.Tests/Devices/NpbModuleTests.cs</files>
  <action>
Create `tests/Simetra.Tests/Devices/NpbModuleTests.cs` following the project's test conventions (xUnit + FluentAssertions).

**Test class structure:**
- Private field: `private readonly NpbModule _sut = new();`
- Use FluentAssertions `.Should()` for all assertions

**Tests to write (minimum -- add more if natural):**

Identity tests:
- `DeviceType_IsNpb` -- `_sut.DeviceType.Should().Be("NPB")`
- `DeviceName_IsNpb2e01` -- `_sut.DeviceName.Should().Be("npb-2e-01")`
- `IpAddress_Is10_0_10_1` -- `_sut.IpAddress.Should().Be("10.0.10.1")`

TrapDefinitions tests:
- `TrapDefinitions_HasTwoDefinitions` -- count == 2
- `TrapDefinitions_ContainsPortLinkUp` -- contains MetricName "port_link_up"
- `TrapDefinitions_ContainsPortLinkDown` -- contains MetricName "port_link_down"
- `TrapDefinitions_PortLinkUp_Has6Varbinds` -- find by MetricName, verify Oids.Count == 6
- `TrapDefinitions_PortLinkDown_Has5Varbinds` -- find by MetricName, verify Oids.Count == 5
- `TrapDefinitions_AllHaveSourceModule` -- all definitions have Source == MetricPollSource.Module
- `TrapDefinitions_AllHaveMetricTypeGauge` -- all trap definitions have MetricType.Gauge

StatePollDefinitions tests:
- `StatePollDefinitions_HasThreeDefinitions` -- count == 3
- `StatePollDefinitions_ContainsRxPackets` -- MetricName "port_rx_packets"
- `StatePollDefinitions_ContainsTxPackets` -- MetricName "port_tx_packets"
- `StatePollDefinitions_ContainsLinkStatus` -- MetricName "port_link_status"
- `StatePollDefinitions_AllHaveSourceModule` -- all have Source == MetricPollSource.Module
- `StatePollDefinitions_RxPacketsAndTxPackets_AreCounterType` -- both are MetricType.Counter
- `StatePollDefinitions_LinkStatus_IsGaugeType` -- MetricType.Gauge
- `StatePollDefinitions_AllHaveIntervalSeconds30` -- IntervalSeconds == 30

EnumMap tests:
- `StatePollDefinitions_LinkStatus_HasEnumMap` -- EnumMap is not null
- `StatePollDefinitions_LinkStatus_EnumMap_HasFiveEntries` -- 5 entries
- `StatePollDefinitions_LinkStatus_EnumMap_MapsCorrectValues` -- verify {-1:"unknown", 0:"down", 1:"up", 2:"receiveDown", 3:"forcedDown"}

OID correctness tests:
- `TrapDefinitions_PortLinkUp_VarbindOids_AllStartWithNpb2ePrefix` -- all OIDs start with "1.3.6.1.4.1.47477.100.4"
- `TrapDefinitions_PortLinkDown_VarbindOids_AllStartWithNpb2ePrefix` -- same check
- `StatePollDefinitions_AllOids_StartWithNpb2ePrefix` -- all poll OIDs start with base prefix
  </action>
  <verify>
All tests pass: `dotnet test tests/Simetra.Tests/Simetra.Tests.csproj --filter "FullyQualifiedName~NpbModuleTests"`
All existing tests still pass: `dotnet test tests/Simetra.Tests/Simetra.Tests.csproj`
  </verify>
  <done>
NpbModuleTests.cs exists with 20+ unit tests covering identity, trap definitions (count, varbinds, source, metric type), state poll definitions (count, source, metric type, intervals), EnumMap (completeness, correct values), and OID correctness. All tests pass, all prior tests unaffected.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/Simetra/Simetra.csproj` -- compiles without errors
2. `dotnet test tests/Simetra.Tests/Simetra.Tests.csproj --filter "FullyQualifiedName~NpbModuleTests"` -- all NpbModule tests pass
3. `dotnet test tests/Simetra.Tests/Simetra.Tests.csproj` -- all existing tests still pass (no regressions)
4. NpbModule has DeviceType "NPB", DeviceName "npb-2e-01", IpAddress "10.0.10.1"
5. TrapDefinitions has exactly 2 entries (portLinkUp with 6 varbinds, portLinkDown with 5 varbinds)
6. StatePollDefinitions has exactly 3 entries (RxPackets, TxPackets, LinkStatus)
7. LinkStatus has EnumMap with 5 entries mapping LinkStatusType values
</verification>

<success_criteria>
- NpbModule.cs compiles and implements IDeviceModule
- All OIDs verified against MIB hierarchy (base prefix 1.3.6.1.4.1.47477.100.4)
- 20+ unit tests covering all requirements NPB-01 through NPB-08
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/12-npb-device-module/12-01-SUMMARY.md`
</output>
