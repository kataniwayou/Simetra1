---
phase: 01-project-foundation-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Simetra.sln
  - src/Simetra/Simetra.csproj
  - src/Simetra/Program.cs
  - src/Simetra/GlobalUsings.cs
  - src/Simetra/appsettings.json
  - src/Simetra/appsettings.Development.json
  - tests/Simetra.Tests/Simetra.Tests.csproj
  - tests/Simetra.Tests/GlobalUsings.cs
  - .editorconfig
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "dotnet build compiles the solution without errors"
    - "dotnet run starts a host and GET /healthz/startup returns 200"
    - "Test project references main project and dotnet test discovers zero tests (no failures)"
    - "Directory structure matches architecture layout with placeholder folders"
  artifacts:
    - path: "Simetra.sln"
      provides: "Solution file referencing both projects"
      contains: "Simetra.csproj"
    - path: "src/Simetra/Simetra.csproj"
      provides: "Main project with Microsoft.NET.Sdk.Web, net9.0, nullable enabled"
      contains: "Microsoft.NET.Sdk.Web"
    - path: "src/Simetra/Program.cs"
      provides: "WebApplication.CreateBuilder entry point with health check skeleton"
      contains: "MapHealthChecks"
    - path: "tests/Simetra.Tests/Simetra.Tests.csproj"
      provides: "Test project with xUnit 2.9.3, FluentAssertions 7.2.0, Moq 4.20.72"
      contains: "FluentAssertions"
  key_links:
    - from: "Simetra.sln"
      to: "src/Simetra/Simetra.csproj"
      via: "Project reference in solution"
      pattern: "Simetra\\.csproj"
    - from: "Simetra.sln"
      to: "tests/Simetra.Tests/Simetra.Tests.csproj"
      via: "Project reference in solution"
      pattern: "Simetra\\.Tests\\.csproj"
    - from: "tests/Simetra.Tests/Simetra.Tests.csproj"
      to: "src/Simetra/Simetra.csproj"
      via: "ProjectReference element"
      pattern: "ProjectReference.*Simetra\\.csproj"
---

<objective>
Scaffold the .NET 9 solution with two projects (Simetra main + Simetra.Tests), establish the directory structure matching the architecture layout, create Program.cs with WebApplication.CreateBuilder and health check skeleton, and set up the test project with xUnit + FluentAssertions + Moq.

Purpose: This is the foundation that all subsequent phases build on. A compiling solution with the correct SDK, directory layout, and test infrastructure.
Output: A solution that compiles, runs, and serves a health endpoint returning 200.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation-configuration/01-RESEARCH.md
@.planning/phases/01-project-foundation-configuration/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create solution, projects, and directory structure</name>
  <files>
    Simetra.sln
    src/Simetra/Simetra.csproj
    tests/Simetra.Tests/Simetra.Tests.csproj
    .editorconfig
    .gitignore
  </files>
  <action>
    1. Create the solution file at the repo root:
       `dotnet new sln -n Simetra -o .` (run from C:\Users\UserL\source\repos\Simetra115Cla)

    2. Create the main project using Web SDK (NOT Worker SDK -- Web SDK provides both BackgroundService and Kestrel for health endpoints):
       `dotnet new web -n Simetra -o src/Simetra --no-https`
       Then edit src/Simetra/Simetra.csproj to ensure it has:
       ```xml
       <Project Sdk="Microsoft.NET.Sdk.Web">
         <PropertyGroup>
           <TargetFramework>net9.0</TargetFramework>
           <OutputType>Exe</OutputType>
           <Nullable>enable</Nullable>
           <ImplicitUsings>enable</ImplicitUsings>
           <RootNamespace>Simetra</RootNamespace>
         </PropertyGroup>
       </Project>
       ```

    3. Create the test project:
       `dotnet new xunit -n Simetra.Tests -o tests/Simetra.Tests`
       Then edit tests/Simetra.Tests/Simetra.Tests.csproj to have the exact package versions:
       ```xml
       <Project Sdk="Microsoft.NET.Sdk">
         <PropertyGroup>
           <TargetFramework>net9.0</TargetFramework>
           <Nullable>enable</Nullable>
           <ImplicitUsings>enable</ImplicitUsings>
           <IsPackable>false</IsPackable>
           <IsTestProject>true</IsTestProject>
         </PropertyGroup>
         <ItemGroup>
           <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.12.0" />
           <PackageReference Include="xunit" Version="2.9.3" />
           <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2" />
           <PackageReference Include="FluentAssertions" Version="7.2.0" />
           <PackageReference Include="Moq" Version="4.20.72" />
           <PackageReference Include="coverlet.collector" Version="6.0.4" />
         </ItemGroup>
         <ItemGroup>
           <ProjectReference Include="..\..\src\Simetra\Simetra.csproj" />
         </ItemGroup>
       </Project>
       ```
       IMPORTANT: FluentAssertions MUST be pinned to 7.2.0 (Apache 2.0 license). Do NOT use 8.x (commercial license).

    4. Add both projects to the solution:
       `dotnet sln add src/Simetra/Simetra.csproj`
       `dotnet sln add tests/Simetra.Tests/Simetra.Tests.csproj`

    5. Create the architecture directory structure with .gitkeep files in empty placeholder folders:
       - src/Simetra/Configuration/          (populated in Plan 02)
       - src/Simetra/Configuration/Validators/ (populated in Plan 02)
       - src/Simetra/Models/                 (populated in Phase 2)
       - src/Simetra/Pipeline/               (populated in Phase 3)
       - src/Simetra/Devices/                (populated in Phase 5)
       - src/Simetra/Services/               (populated in Phase 3)
       - src/Simetra/Jobs/                   (populated in Phase 6)
       - src/Simetra/Health/                 (populated in Phase 9)
       - src/Simetra/Middleware/             (populated in Phase 3)
       - src/Simetra/Telemetry/             (populated in Phase 7)
       - src/Simetra/Extensions/            (populated in Plan 02)
       - tests/Simetra.Tests/Configuration/ (populated in Plan 03)

    6. Create .editorconfig at the repo root with standard .NET rules:
       - indent_style = space, indent_size = 4
       - charset = utf-8
       - end_of_line = crlf (Windows project)
       - insert_final_newline = true
       - Allman brace style (csharp_new_line_before_open_brace = all)
       - Nullable warnings enabled
       - Sort usings with System first

    7. Create or update .gitignore for .NET projects (bin/, obj/, .vs/, *.user, etc.). Use `dotnet new gitignore` if one does not exist already.

    8. Delete any auto-generated placeholder test files (like UnitTest1.cs) from the test project template.
  </action>
  <verify>
    Run `dotnet restore` from solution root -- must succeed with no errors.
    Run `dotnet build` from solution root -- must compile with 0 errors.
    Verify directory structure exists: `dir src\Simetra\Configuration`, `dir src\Simetra\Models`, etc.
  </verify>
  <done>
    Solution compiles. Both projects are in the solution. All architecture directories exist. .editorconfig and .gitignore are present. Test project has correct package versions (FluentAssertions 7.2.0 pinned).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Program.cs, GlobalUsings, and appsettings.json skeleton</name>
  <files>
    src/Simetra/Program.cs
    src/Simetra/GlobalUsings.cs
    src/Simetra/appsettings.json
    src/Simetra/appsettings.Development.json
    tests/Simetra.Tests/GlobalUsings.cs
  </files>
  <action>
    1. Create src/Simetra/Program.cs with WebApplication.CreateBuilder pattern:
       ```csharp
       var builder = WebApplication.CreateBuilder(args);

       // Configuration binding will be added in Plan 02
       // builder.Services.AddSimetraConfiguration(builder.Configuration);

       builder.Services.AddHealthChecks();

       var app = builder.Build();

       // Health probe endpoints (skeleton -- actual checks added in Phase 9)
       app.MapHealthChecks("/healthz/startup");
       app.MapHealthChecks("/healthz/ready");
       app.MapHealthChecks("/healthz/live");

       app.Run();
       ```
       Do NOT add a namespace or class wrapper -- use top-level statements (.NET 9 convention).
       Do NOT add "app.MapGet("/"..." -- only health check endpoints.

    2. Create src/Simetra/GlobalUsings.cs:
       ```csharp
       // Simetra global usings
       // Microsoft.NET.Sdk.Web provides implicit usings for common ASP.NET Core namespaces.
       // Add project-specific usings here as the project grows.
       global using Simetra.Configuration;
       ```

    3. Create src/Simetra/appsettings.json with the COMPLETE configuration structure matching the design doc Section 9. Use flat sections (no wrapping). Include sample device entries showing MetricPolls structure (per CONTEXT.md decision). Use the exact JSON from the RESEARCH.md Pattern 3 example as the baseline, ensuring ALL 12 config sections are present:
       - Site (Name, PodIdentity)
       - Lease (Name, Namespace, RenewIntervalSeconds, DurationSeconds)
       - SnmpListener (BindAddress, Port, CommunityString, Version)
       - HeartbeatJob (IntervalSeconds)
       - CorrelationJob (IntervalSeconds)
       - Liveness (GraceMultiplier)
       - Channels (BoundedCapacity)
       - Devices[] with sample entries showing MetricPolls[] with Oids[] (MetricName, MetricType as string enum, Oids with Oid/PropertyName/Role as string enum, IntervalSeconds)
       - Otlp (Endpoint, ServiceName)
       - Logging (LogLevel:Default, EnableConsole)

       Include at least 2 sample devices: one with MetricPolls (router-core-1) and one without MetricPolls (switch-floor-2) to demonstrate both cases.

    4. Create src/Simetra/appsettings.Development.json with development overrides:
       ```json
       {
         "Logging": {
           "LogLevel": {
             "Default": "Debug"
           },
           "EnableConsole": true
         },
         "Otlp": {
           "Endpoint": "http://localhost:4317"
         }
       }
       ```

    5. Create tests/Simetra.Tests/GlobalUsings.cs:
       ```csharp
       // Test project global usings
       global using Xunit;
       global using FluentAssertions;
       global using Moq;
       global using Simetra.Configuration;
       ```

    6. Run `dotnet build` to verify everything compiles.

    7. Run `dotnet run --project src/Simetra` briefly (use timeout or Ctrl+C) and verify with curl or similar that GET http://localhost:5000/healthz/startup returns 200. If the port is different, check launchSettings.json or use whatever port the app binds to. After verification, stop the running process.
  </action>
  <verify>
    Run `dotnet build` -- 0 errors.
    Run the app briefly and confirm GET /healthz/startup returns HTTP 200 (Healthy).
    Confirm appsettings.json contains all 12 configuration sections.
    Run `dotnet test` -- 0 tests discovered (no failures, no test files yet).
  </verify>
  <done>
    Program.cs runs with WebApplication.CreateBuilder, health endpoints return 200, appsettings.json has all 12 sections with sample device data, GlobalUsings files exist for both projects, and dotnet test runs clean with no tests.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build` from solution root compiles with 0 errors and 0 warnings (or only nullable-related info)
2. `dotnet run --project src/Simetra` starts and serves health endpoints on HTTP
3. GET /healthz/startup returns 200
4. `dotnet test` runs clean (0 tests, 0 failures)
5. Directory structure has all architecture folders: Configuration/, Models/, Pipeline/, Devices/, Services/, Jobs/, Health/, Middleware/, Telemetry/, Extensions/
6. FluentAssertions version in Simetra.Tests.csproj is exactly 7.2.0
</verification>

<success_criteria>
- Solution compiles and runs
- Health endpoint returns 200
- All architecture directories exist
- appsettings.json contains all 12 configuration sections with sample data
- Test project is wired and ready for tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation-configuration/01-01-SUMMARY.md`
</output>
