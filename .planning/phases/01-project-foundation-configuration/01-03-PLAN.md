---
phase: 01-project-foundation-configuration
plan: 03
type: tdd
wave: 3
depends_on: ["01-02"]
files_modified:
  - tests/Simetra.Tests/Configuration/ConfigurationBindingTests.cs
  - tests/Simetra.Tests/Configuration/SiteOptionsValidationTests.cs
  - tests/Simetra.Tests/Configuration/SnmpListenerOptionsValidationTests.cs
  - tests/Simetra.Tests/Configuration/DevicesOptionsValidationTests.cs
  - tests/Simetra.Tests/Configuration/LeaseOptionsValidationTests.cs
  - tests/Simetra.Tests/Configuration/OtlpOptionsValidationTests.cs
autonomous: true

must_haves:
  truths:
    - "Tests prove valid appsettings.json binds all 12 sections into correctly typed Options objects"
    - "Tests prove missing required fields are caught by validation with descriptive errors"
    - "Tests prove nested Devices[].MetricPolls[].Oids[] validates recursively"
    - "Tests prove empty Devices[] is valid"
    - "Tests prove unknown DeviceType causes validation failure"
    - "Tests prove MetricPollOptions.Source is set to Configuration (not from JSON)"
    - "Tests prove PodIdentity defaults from environment when not configured"
    - "All tests pass with dotnet test"
  artifacts:
    - path: "tests/Simetra.Tests/Configuration/ConfigurationBindingTests.cs"
      provides: "Tests for complete config binding and deserialization"
      contains: "ConfigurationBindingTests"
    - path: "tests/Simetra.Tests/Configuration/DevicesOptionsValidationTests.cs"
      provides: "Tests for nested Devices/MetricPolls/Oids validation"
      contains: "DevicesOptionsValidationTests"
    - path: "tests/Simetra.Tests/Configuration/SiteOptionsValidationTests.cs"
      provides: "Tests for Site options validation and PodIdentity defaulting"
      contains: "SiteOptionsValidationTests"
  key_links:
    - from: "tests/Simetra.Tests/Configuration/ConfigurationBindingTests.cs"
      to: "src/Simetra/Configuration/DevicesOptions.cs"
      via: "Builds IConfiguration from JSON, binds to Options, asserts property values"
      pattern: "GetSection.*Bind|ConfigurationBuilder"
    - from: "tests/Simetra.Tests/Configuration/DevicesOptionsValidationTests.cs"
      to: "src/Simetra/Configuration/Validators/DevicesOptionsValidator.cs"
      via: "Calls Validate() directly and asserts result"
      pattern: "validator\\.Validate"
---

<objective>
Write configuration validation tests proving that all Options classes bind correctly from JSON, validators catch invalid config, nested Devices/MetricPolls/Oids validates recursively, and edge cases (empty Devices, unknown DeviceType, Source auto-set) work as designed.

Purpose: Config is the primary deliverable of Phase 1. Testing it immediately validates the foundation. Building 9 phases on potentially broken config is wasteful. These ~15-20 tests serve as a regression safety net for the entire configuration layer.
Output: Passing test suite covering configuration binding and validation.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation-configuration/01-RESEARCH.md
@.planning/phases/01-project-foundation-configuration/01-CONTEXT.md
@.planning/phases/01-project-foundation-configuration/01-02-SUMMARY.md
</context>

<feature>
  <name>Configuration Validation Test Suite</name>
  <files>
    tests/Simetra.Tests/Configuration/ConfigurationBindingTests.cs
    tests/Simetra.Tests/Configuration/SiteOptionsValidationTests.cs
    tests/Simetra.Tests/Configuration/SnmpListenerOptionsValidationTests.cs
    tests/Simetra.Tests/Configuration/DevicesOptionsValidationTests.cs
    tests/Simetra.Tests/Configuration/LeaseOptionsValidationTests.cs
    tests/Simetra.Tests/Configuration/OtlpOptionsValidationTests.cs
  </files>
  <behavior>
    Test naming convention: MethodName_Scenario_ExpectedBehavior
    Use FluentAssertions for assertions.
    Build IConfiguration from in-memory JSON dictionaries (not file-based) for isolation.

    **ConfigurationBindingTests.cs** - End-to-end binding verification:
    - Bind_WithCompleteValidJson_PopulatesAllSiteProperties
    - Bind_WithCompleteValidJson_PopulatesAllLeaseProperties
    - Bind_WithCompleteValidJson_PopulatesAllSnmpListenerProperties
    - Bind_WithCompleteValidJson_PopulatesDevicesWithNestedMetricPolls
    - Bind_WithDeviceMetricPolls_DeserializesOidEntriesWithRoles (verify OidRole.Metric and OidRole.Label bind from strings)
    - Bind_WithDeviceMetricPolls_DeserializesMetricTypeFromString (verify MetricType.Gauge and MetricType.Counter bind from strings)
    - Bind_WithEnumMap_DeserializesDictionaryCorrectly (verify Dictionary<int, string> binds)

    **SiteOptionsValidationTests.cs**:
    - Validate_WhenNameIsMissing_ReturnsFailure
    - Validate_WhenNameIsPresent_ReturnsSuccess
    - PostConfigure_WhenPodIdentityIsNull_DefaultsToMachineName (test PostConfigure behavior)

    **SnmpListenerOptionsValidationTests.cs**:
    - Validate_WhenVersionIsNotV2c_ReturnsFailure
    - Validate_WhenAllFieldsValid_ReturnsSuccess
    - Validate_WhenCommunityStringMissing_ReturnsFailure

    **DevicesOptionsValidationTests.cs** (MOST IMPORTANT - nested validation):
    - Validate_WhenDevicesEmpty_ReturnsSuccess (empty array is valid)
    - Validate_WhenDeviceNameMissing_ReturnsFailure
    - Validate_WhenDeviceIpAddressMissing_ReturnsFailure
    - Validate_WhenDeviceTypeUnknown_ReturnsFailure
    - Validate_WhenMetricPollMetricNameMissing_ReturnsFailure
    - Validate_WhenMetricPollIntervalSecondsZero_ReturnsFailure
    - Validate_WhenMetricPollOidsEmpty_ReturnsFailure
    - Validate_WhenOidEntryOidMissing_ReturnsFailure
    - Validate_WithValidDeviceAndMetricPolls_ReturnsSuccess
    - Validate_ReturnsAllErrorsNotJustFirst (collects all errors)

    **LeaseOptionsValidationTests.cs**:
    - Validate_WhenDurationLessThanRenewInterval_ReturnsFailure
    - Validate_WhenAllFieldsValid_ReturnsSuccess

    **OtlpOptionsValidationTests.cs**:
    - Validate_WhenEndpointMissing_ReturnsFailure
    - Validate_WhenAllFieldsValid_ReturnsSuccess

    Testing pattern for validators: Instantiate the validator directly, create options objects in code, call validator.Validate(null, options), assert ValidateOptionsResult.

    Testing pattern for binding: Use ConfigurationBuilder with AddInMemoryCollection, build IConfiguration, call GetSection().Bind() or Get<T>(), assert property values with FluentAssertions.
  </behavior>
  <implementation>
    Follow TDD red-green-refactor:

    RED: Write all test files with all test methods. Run `dotnet test` -- all tests should pass immediately since the implementation exists from Plan 02. If any test FAILS, that reveals a bug in the implementation.

    GREEN: If any tests fail, fix the implementation code (Options classes or validators) to make tests pass.

    REFACTOR: Clean up test code if needed. Ensure consistent patterns across all test files.

    Since this is Phase 1 and we are writing tests AFTER implementation (the user decided to include tests in Phase 1 rather than deferring to Phase 10), the TDD cycle is inverted: we write tests to validate existing code. Any test failure is a bug to fix.

    Use this helper pattern for building config from dictionaries:
    ```csharp
    private static IConfiguration BuildConfig(Dictionary<string, string?> values)
    {
        return new ConfigurationBuilder()
            .AddInMemoryCollection(values)
            .Build();
    }
    ```

    For DevicesOptions binding test, use the array binding syntax:
    ```csharp
    var config = BuildConfig(new Dictionary<string, string?>
    {
        ["Devices:0:Name"] = "router-1",
        ["Devices:0:IpAddress"] = "10.0.1.1",
        ["Devices:0:DeviceType"] = "router",
        ["Devices:0:MetricPolls:0:MetricName"] = "cpu",
        ["Devices:0:MetricPolls:0:MetricType"] = "Gauge",
        ["Devices:0:MetricPolls:0:IntervalSeconds"] = "30",
        ["Devices:0:MetricPolls:0:Oids:0:Oid"] = "1.3.6.1.4.1.9999.1.3.1.0",
        ["Devices:0:MetricPolls:0:Oids:0:PropertyName"] = "cpu_util",
        ["Devices:0:MetricPolls:0:Oids:0:Role"] = "Metric",
    });
    ```
  </implementation>
</feature>

<verification>
1. `dotnet test` passes with all tests green
2. Test count is approximately 15-25 tests
3. Tests cover: binding, required field validation, range validation, nested validation, enum deserialization, empty Devices, unknown DeviceType, Source auto-set, PodIdentity default
4. No test uses external files -- all use in-memory configuration
</verification>

<success_criteria>
- All tests pass with `dotnet test`
- Config binding tests prove all 12 sections deserialize correctly
- Validator tests prove fail-fast behavior with descriptive errors
- Nested Devices/MetricPolls/Oids validation is proven
- Edge cases covered: empty Devices, unknown DeviceType, Source field auto-set
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation-configuration/01-03-SUMMARY.md`
</output>
