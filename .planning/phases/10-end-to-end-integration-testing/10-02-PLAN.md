---
phase: 10-end-to-end-integration-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Simetra.Tests/Pipeline/StateVectorServiceTests.cs
  - tests/Simetra.Tests/Pipeline/ProcessingCoordinatorTests.cs
  - tests/Simetra.Tests/Processing/MetricFactoryTests.cs
  - tests/Simetra.Tests/Health/LivenessVectorServiceTests.cs
  - tests/Simetra.Tests/Health/LivenessHealthCheckTests.cs
autonomous: true

must_haves:
  truths:
    - "StateVectorService.Update creates and overwrites entries with composite key 'deviceName:metricName', GetEntry returns null for missing keys, GetAllEntries returns snapshot"
    - "ProcessingCoordinator routes Source=Module to both branches (metrics + state vector), Source=Configuration to metrics only, and branch failures are independent"
    - "MetricFactory enforces base labels (site, device_name, device_ip, device_type) on every measurement and appends dynamic Role:Label values"
    - "LivenessVectorService.Stamp records timestamp retrievable by GetStamp, GetAllStamps returns defensive snapshot"
    - "LivenessHealthCheck returns Healthy when all stamps are fresh, Unhealthy with diagnostic data when any stamp exceeds interval x GraceMultiplier"
  artifacts:
    - path: "tests/Simetra.Tests/Pipeline/StateVectorServiceTests.cs"
      provides: "State Vector CRUD and composite key tests"
      contains: "StateVectorServiceTests"
    - path: "tests/Simetra.Tests/Pipeline/ProcessingCoordinatorTests.cs"
      provides: "Source-based routing and branch isolation tests"
      contains: "ProcessingCoordinatorTests"
    - path: "tests/Simetra.Tests/Processing/MetricFactoryTests.cs"
      provides: "Base label enforcement and metric recording tests"
      contains: "MetricFactoryTests"
    - path: "tests/Simetra.Tests/Health/LivenessVectorServiceTests.cs"
      provides: "Liveness vector stamp/retrieve tests"
      contains: "LivenessVectorServiceTests"
    - path: "tests/Simetra.Tests/Health/LivenessHealthCheckTests.cs"
      provides: "Liveness health check staleness detection tests"
      contains: "LivenessHealthCheckTests"
  key_links:
    - from: "tests/Simetra.Tests/Pipeline/ProcessingCoordinatorTests.cs"
      to: "ProcessingCoordinator"
      via: "Real instance with mocked IMetricFactory + IStateVectorService"
      pattern: "new ProcessingCoordinator"
    - from: "tests/Simetra.Tests/Processing/MetricFactoryTests.cs"
      to: "MetricFactory + MeterListener"
      via: "Real MetricFactory with MeterListener to capture measurements"
      pattern: "MeterListener"
    - from: "tests/Simetra.Tests/Health/LivenessHealthCheckTests.cs"
      to: "LivenessHealthCheck"
      via: "Real instance with real/mock liveness vector and interval registry"
      pattern: "new LivenessHealthCheck"
---

<objective>
Unit tests for processing pipeline, scheduling support, and liveness detection: StateVectorService, ProcessingCoordinator source-based routing, MetricFactory base label enforcement, LivenessVectorService stamping, and LivenessHealthCheck staleness detection.

Purpose: Covers TEST-04 (state vector + source routing), TEST-05 (MetricFactory base labels), TEST-06 (liveness vector + staleness detection). These are the data processing and operational monitoring tests.

Output: Five new test files in Pipeline/, Processing/, and Health/ subdirectories covering state management, source routing, metric instrumentation, and liveness probing.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-end-to-end-integration-testing/10-RESEARCH.md
@src/Simetra/Pipeline/StateVectorService.cs
@src/Simetra/Pipeline/StateVectorEntry.cs
@src/Simetra/Pipeline/IStateVectorService.cs
@src/Simetra/Pipeline/ProcessingCoordinator.cs
@src/Simetra/Pipeline/IProcessingCoordinator.cs
@src/Simetra/Pipeline/MetricFactory.cs
@src/Simetra/Pipeline/IMetricFactory.cs
@src/Simetra/Pipeline/LivenessVectorService.cs
@src/Simetra/Pipeline/ILivenessVectorService.cs
@src/Simetra/Pipeline/JobIntervalRegistry.cs
@src/Simetra/Pipeline/IJobIntervalRegistry.cs
@src/Simetra/HealthChecks/LivenessHealthCheck.cs
@src/Simetra/Models/ExtractionResult.cs
@src/Simetra/Models/PollDefinitionDto.cs
@src/Simetra/Models/OidEntryDto.cs
@src/Simetra/Pipeline/DeviceInfo.cs
@src/Simetra/Configuration/SiteOptions.cs
@src/Simetra/Configuration/MetricType.cs
@src/Simetra/Configuration/MetricPollSource.cs
@src/Simetra/Configuration/LivenessOptions.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: StateVectorService, ProcessingCoordinator, and MetricFactory tests</name>
  <files>
    tests/Simetra.Tests/Pipeline/StateVectorServiceTests.cs
    tests/Simetra.Tests/Pipeline/ProcessingCoordinatorTests.cs
    tests/Simetra.Tests/Processing/MetricFactoryTests.cs
  </files>
  <action>
    Create the Processing/ directory under tests/Simetra.Tests/ if it does not exist.

    **StateVectorServiceTests.cs** (namespace Simetra.Tests.Pipeline):
    - Instantiate real StateVectorService with `Mock.Of&lt;ILogger&lt;StateVectorService&gt;&gt;()`
    - Helper `MakeResult(string metricName, MetricPollSource source)` that creates ExtractionResult with a PollDefinitionDto
    - Tests:
      1. `Update_CreatesNewEntry` -- update "dev1":"metric1", GetEntry returns non-null with correct Result and CorrelationId
      2. `Update_OverwritesExistingEntry` -- update same key twice, GetEntry returns second value
      3. `GetEntry_MissingKey_ReturnsNull` -- query non-existent key returns null
      4. `GetAllEntries_ReturnsSnapshot` -- update two entries, GetAllEntries returns dictionary with both composite keys "dev1:metric1" and "dev2:metric2"
      5. `Update_TimestampIsRecent` -- verify entry.Timestamp is within 1 second of DateTimeOffset.UtcNow using BeCloseTo

    **ProcessingCoordinatorTests.cs** (namespace Simetra.Tests.Pipeline):
    - Create real ProcessingCoordinator with `Mock&lt;IMetricFactory&gt;` and `Mock&lt;IStateVectorService&gt;` and `Mock.Of&lt;ILogger&gt;`
    - Helper `MakeResult(MetricPollSource source)` that creates ExtractionResult with PollDefinitionDto having given Source
    - Helper `MakeDevice()` that creates DeviceInfo with test values
    - Tests:
      1. `Process_SourceModule_CallsBothBranches` -- Source=Module calls RecordMetrics AND Update
      2. `Process_SourceConfiguration_CallsMetricsOnly` -- Source=Configuration calls RecordMetrics, NEVER calls Update (verify Times.Never)
      3. `Process_BranchAFailure_DoesNotBlockBranchB` -- Setup RecordMetrics to throw, verify Update is still called (Source=Module)
      4. `Process_BranchBFailure_DoesNotBlockBranchA` -- Setup Update to throw, verify RecordMetrics is still called (Source=Module)
    - Use Moq Verify with Times.Once and Times.Never for branch activation assertions

    **MetricFactoryTests.cs** (namespace Simetra.Tests.Processing):
    - This is the most complex test in this plan. Uses `System.Diagnostics.Metrics.MeterListener` to capture recorded measurements.
    - Setup: Mock `IMeterFactory` to return a real `Meter("Simetra.Metrics.Test")`. Create `MetricFactory` with the mock IMeterFactory, `Options.Create(new SiteOptions { Name = "test-site" })`, and mock logger. Set up `MeterListener` to capture measurements.
    - Important: Mock IMeterFactory like this:
      ```
      var meter = new Meter("Simetra.Metrics.Test");
      var mockMeterFactory = new Mock&lt;IMeterFactory&gt;();
      mockMeterFactory.Setup(f => f.Create(It.IsAny&lt;MeterOptions&gt;())).Returns(meter);
      ```
      Note: IMeterFactory.Create takes MeterOptions, not string. The MetricFactory calls `meterFactory.Create("Simetra.Metrics")` which uses an implicit conversion or overload. Check the actual signature -- `IMeterFactory.Create(MeterOptions options)` where the string overload may be an extension method. If so, mock the `Create(MeterOptions)` method and match `It.Is&lt;MeterOptions&gt;(o => o.Name == "Simetra.Metrics")`.
    - MeterListener setup pattern:
      ```csharp
      var recordedTags = new List&lt;KeyValuePair&lt;string, object?&gt;&gt;();
      using var listener = new MeterListener();
      listener.InstrumentPublished = (instrument, meterListener) =&gt;
      {
          meterListener.EnableMeasurementEvents(instrument);
      };
      listener.SetMeasurementEventCallback&lt;long&gt;((instrument, value, tags, state) =&gt;
      {
          foreach (var tag in tags)
              recordedTags.Add(tag);
      });
      listener.Start();
      ```
    - Tests:
      1. `RecordMetrics_IncludesBaseLabels` -- create ExtractionResult with one Metric entry, call RecordMetrics, verify recordedTags contains site, device_name, device_ip, device_type with correct values
      2. `RecordMetrics_AppendsDynamicLabels` -- create ExtractionResult with Labels {"status": "up"}, verify recordedTags includes "status" = "up" in addition to 4 base labels
      3. `RecordMetrics_MetricNameFormat` -- create ExtractionResult with MetricName "simetra_heartbeat" and PropertyName "uptime", verify MeterListener receives instrument named "simetra_heartbeat_uptime"
      4. `RecordMetrics_GaugeType_RecordsValue` -- MetricType.Gauge produces Gauge instrument
      5. `RecordMetrics_CounterType_AddsValue` -- MetricType.Counter produces Counter instrument
    - IMPORTANT: Dispose the Meter in test cleanup to avoid test pollution. Use IDisposable or dispose in each test.
  </action>
  <verify>
    `dotnet test tests/Simetra.Tests/ --filter "FullyQualifiedName~StateVectorServiceTests|FullyQualifiedName~ProcessingCoordinatorTests|FullyQualifiedName~MetricFactoryTests"` -- all tests pass.
  </verify>
  <done>
    StateVectorService tested for create, overwrite, missing key, snapshot, and timestamp freshness. ProcessingCoordinator tested for Module/Configuration routing and independent branch isolation. MetricFactory tested for base label enforcement, dynamic label appending, metric name format, and Gauge vs Counter instrument types. Covers TEST-04, TEST-05.
  </done>
</task>

<task type="auto">
  <name>Task 2: LivenessVectorService and LivenessHealthCheck tests</name>
  <files>
    tests/Simetra.Tests/Health/LivenessVectorServiceTests.cs
    tests/Simetra.Tests/Health/LivenessHealthCheckTests.cs
  </files>
  <action>
    Create the Health/ directory under tests/Simetra.Tests/ if it does not exist.

    **LivenessVectorServiceTests.cs** (namespace Simetra.Tests.Health):
    - Instantiate real LivenessVectorService (parameterless constructor, no mocks needed)
    - Tests:
      1. `Stamp_RecordsTimestamp` -- stamp "heartbeat", GetStamp returns non-null
      2. `Stamp_TimestampIsRecent` -- stamp, verify GetStamp is within 1 second of DateTimeOffset.UtcNow using BeCloseTo
      3. `GetStamp_NeverStamped_ReturnsNull` -- query "unknown-job" returns null
      4. `Stamp_OverwritesPreviousValue` -- stamp "heartbeat" twice (with brief delay or different assertion), verify the latest stamp is returned
      5. `GetAllStamps_ReturnsAllEntries` -- stamp "heartbeat" and "correlation", GetAllStamps returns dictionary with 2 entries
      6. `GetAllStamps_ReturnsDefensiveCopy` -- GetAllStamps, stamp a new job, call GetAllStamps again, verify first snapshot does not contain the new job (proving defensive copy)

    **LivenessHealthCheckTests.cs** (namespace Simetra.Tests.Health):
    - For "fresh stamps" test: use real LivenessVectorService and real JobIntervalRegistry (both parameterless constructors)
    - For "stale stamps" test: use `Mock&lt;ILivenessVectorService&gt;` to return old timestamps
    - Options: `Options.Create(new LivenessOptions { GraceMultiplier = 3.0 })`
    - Logger: `Mock.Of&lt;ILogger&lt;LivenessHealthCheck&gt;&gt;()`
    - Tests:
      1. `CheckHealth_AllFreshStamps_ReturnsHealthy` -- stamp "heartbeat" (now), register interval 15s, grace 3.0 -> threshold 45s, stamp age ~0s -> Healthy
      2. `CheckHealth_StaleStamp_ReturnsUnhealthy` -- mock GetAllStamps to return stamp from 120s ago, register interval 15s, grace 3.0 -> threshold 45s, age 120s -> Unhealthy
      3. `CheckHealth_UnhealthyIncludesStaleData` -- verify Unhealthy result.Data contains the stale job key with age/threshold info
      4. `CheckHealth_UnknownJobKey_Skipped` -- mock GetAllStamps with job "unknown" not in interval registry, verify Healthy (unknown jobs are ignored)
      5. `CheckHealth_NoStamps_ReturnsHealthy` -- empty stamps dictionary -> Healthy (no stamps = no stale entries)
      6. `CheckHealth_MixedFreshAndStale_ReturnsUnhealthy` -- one fresh stamp, one stale stamp -> Unhealthy (any stale = overall unhealthy)
    - For stale stamp mocking: `mockLiveness.Setup(l => l.GetAllStamps()).Returns(new Dictionary&lt;string, DateTimeOffset&gt; { ["heartbeat"] = DateTimeOffset.UtcNow.AddSeconds(-120) }.AsReadOnly())`
    - HealthCheckContext: use `new HealthCheckContext()` (current health checks do not access context.Registration)
  </action>
  <verify>
    `dotnet test tests/Simetra.Tests/ --filter "FullyQualifiedName~LivenessVectorServiceTests|FullyQualifiedName~LivenessHealthCheckTests"` -- all tests pass.
  </verify>
  <done>
    LivenessVectorService tested for stamp recording, null for unstamped, overwrite, GetAllStamps snapshot, and defensive copy. LivenessHealthCheck tested for all-fresh Healthy, stale Unhealthy with diagnostic data, unknown job skipping, empty stamps Healthy, and mixed fresh/stale Unhealthy. Covers TEST-06.
  </done>
</task>

</tasks>

<verification>
1. `dotnet test tests/Simetra.Tests/` -- all tests pass (existing 60 + all new tests from this plan)
2. `dotnet test tests/Simetra.Tests/ --filter "FullyQualifiedName~StateVectorServiceTests"` -- 5 tests pass
3. `dotnet test tests/Simetra.Tests/ --filter "FullyQualifiedName~ProcessingCoordinatorTests"` -- 4 tests pass
4. `dotnet test tests/Simetra.Tests/ --filter "FullyQualifiedName~MetricFactoryTests"` -- 5 tests pass
5. `dotnet test tests/Simetra.Tests/ --filter "FullyQualifiedName~LivenessVectorServiceTests"` -- 6 tests pass
6. `dotnet test tests/Simetra.Tests/ --filter "FullyQualifiedName~LivenessHealthCheckTests"` -- 6 tests pass
7. Grep for `MeterListener` in MetricFactoryTests.cs -- confirms measurement capture approach (TEST-05)
8. Grep for `Times.Never` in ProcessingCoordinatorTests.cs -- confirms Source=Configuration skips state vector (TEST-04)
</verification>

<success_criteria>
- All new tests pass alongside existing tests (no regressions)
- StateVectorService: 5 tests covering create/overwrite/missing/snapshot/timestamp
- ProcessingCoordinator: 4 tests covering Module routing, Configuration routing, and branch isolation
- MetricFactory: 5 tests covering base labels, dynamic labels, metric name format, Gauge, Counter
- LivenessVectorService: 6 tests covering stamp/null/overwrite/snapshot/defensive-copy
- LivenessHealthCheck: 6 tests covering fresh/stale/data/unknown/empty/mixed
- MeterListener used for MetricFactory tests to verify label enforcement without OTLP
</success_criteria>

<output>
After completion, create `.planning/phases/10-end-to-end-integration-testing/10-02-SUMMARY.md`
</output>
