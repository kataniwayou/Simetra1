---
phase: 10-end-to-end-integration-testing
plan: 04
type: execute
wave: 2
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - tests/Simetra.Tests/Integration/HeartbeatLoopbackTests.cs
autonomous: true

must_haves:
  truths:
    - "All unit tests from plans 01-03 pass before integration test runs (prerequisite)"
    - "Full test suite runs green with dotnet test (existing 60 + all new unit tests + integration test)"
    - "Test count is verified to ensure no tests were silently skipped or excluded"
  artifacts:
    - path: "tests/Simetra.Tests/Integration/HeartbeatLoopbackTests.cs"
      provides: "End-to-end integration verification test"
      contains: "HeartbeatLoopbackTests"
  key_links:
    - from: "tests/Simetra.Tests/Integration/HeartbeatLoopbackTests.cs"
      to: "Full pipeline (SnmpExtractorService -> ProcessingCoordinator -> StateVectorService -> MetricFactory)"
      via: "In-process wiring without network"
      pattern: "SnmpExtractorService.*ProcessingCoordinator"
---

<objective>
End-to-end heartbeat loopback integration test proving the full pipeline processes data correctly, plus final test suite verification ensuring all unit tests pass together.

Purpose: Validates Phase 10 success criteria #1 (heartbeat loopback end-to-end) by wiring real service instances in-process (no UDP, no Quartz -- just data flow through extraction, processing, state vector, and metrics). Then runs the complete test suite to confirm all tests pass with zero failures.

Output: One integration test file proving end-to-end data flow, plus verified green test suite.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-end-to-end-integration-testing/10-RESEARCH.md
@src/Simetra/Services/SnmpExtractorService.cs
@src/Simetra/Pipeline/ProcessingCoordinator.cs
@src/Simetra/Pipeline/StateVectorService.cs
@src/Simetra/Pipeline/MetricFactory.cs
@src/Simetra/Devices/SimetraModule.cs
@src/Simetra/Models/PollDefinitionDto.cs
@src/Simetra/Models/ExtractionResult.cs
@src/Simetra/Pipeline/DeviceInfo.cs
@src/Simetra/Configuration/SiteOptions.cs
@src/Simetra/Configuration/MetricType.cs
@src/Simetra/Configuration/MetricPollSource.cs
@src/Simetra/Configuration/OidRole.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Heartbeat loopback integration test and full suite verification</name>
  <files>
    tests/Simetra.Tests/Integration/HeartbeatLoopbackTests.cs
  </files>
  <action>
    Create the Integration/ directory under tests/Simetra.Tests/ if it does not exist.

    **HeartbeatLoopbackTests.cs** (namespace Simetra.Tests.Integration):
    This test proves the heartbeat data flow through the pipeline WITHOUT network or Quartz -- it wires real service instances in-process and passes data through the extraction and processing layers.

    The test simulates what HeartbeatJob does: creates SNMP varbinds matching the Simetra heartbeat definition, feeds them through the extractor, and passes the result to the ProcessingCoordinator which records metrics and updates the State Vector.

    **Test setup:**
    - Read the heartbeat trap definition from SimetraModule: instantiate SimetraModule and get TrapDefinitions[0] (the heartbeat definition)
    - Create real SnmpExtractorService with mock logger
    - Create real StateVectorService with mock logger
    - Create real MetricFactory with mock IMeterFactory (returning a real Meter), Options.Create(SiteOptions), and mock logger
    - Create real ProcessingCoordinator with the real MetricFactory and real StateVectorService
    - Create a DeviceInfo for the Simetra virtual device matching module config

    **Test flow:**
    1. Build SNMP varbinds matching the SimetraModule heartbeat OID(s) -- use the OIDs from the trap definition. The heartbeat OID is `SimetraModule.HeartbeatOid`. Create `new Variable(new ObjectIdentifier(heartbeatOid), new Integer32(1))` (value=1 for alive)
    2. Call `extractor.Extract(varbinds, heartbeatDefinition)` -- produces ExtractionResult
    3. Verify ExtractionResult has at least one Metric entry (heartbeat value)
    4. Call `coordinator.Process(result, simetraDevice, "test-corr-1")` -- Branch A records metric, Branch B updates State Vector
    5. Query StateVectorService.GetEntry("simetra", heartbeatDefinition.MetricName) -- verify non-null entry
    6. Verify entry.CorrelationId == "test-corr-1"
    7. Verify entry.Timestamp is recent (BeCloseTo now)

    **Tests:**
    1. `HeartbeatData_FlowsThroughPipeline_ProducesStateVectorEntry` -- full flow as described above
    2. `HeartbeatExtraction_ProducesMetricValues` -- extract heartbeat varbinds, verify ExtractionResult.Metrics contains expected property name(s) with numeric values

    **Important implementation notes:**
    - Read SimetraModule source to find the exact OID value (HeartbeatOid const) and the heartbeat PollDefinitionDto structure (metric name, OID entries)
    - The heartbeat definition has Source=Module, so ProcessingCoordinator will call BOTH branches
    - Use real IMeterFactory mock pattern: `var meter = new Meter("Simetra.Metrics.Test"); mockMeterFactory.Setup(f => f.Create(It.IsAny&lt;MeterOptions&gt;())).Returns(meter);`
    - Dispose the Meter after test

    After the integration test file is created, run the FULL test suite:
    ```
    dotnet test tests/Simetra.Tests/ --verbosity normal
    ```
    Verify:
    - Zero failures
    - Zero errors
    - Total test count includes all existing (60) + all new unit tests from Plans 01-03 + integration tests from this plan
    - Log the final test count for the SUMMARY

    If any tests fail, diagnose and fix before proceeding.
  </action>
  <verify>
    `dotnet test tests/Simetra.Tests/ --verbosity normal` -- ALL tests pass with zero failures. Grep output for "Passed" count to confirm total.
  </verify>
  <done>
    Heartbeat loopback integration test proves data flows from SNMP varbinds through extractor, into ProcessingCoordinator (both branches), producing a State Vector entry with correct correlationId. Full test suite passes with zero failures -- all existing tests + all new unit tests + integration test are green.
  </done>
</task>

</tasks>

<verification>
1. `dotnet test tests/Simetra.Tests/ --verbosity normal` -- ALL tests pass
2. Verify test count is >= 60 (existing) + ~34 (new unit tests from Plans 01-03) + ~2 (integration) = ~96+ total
3. Grep for `StateVectorService.GetEntry` in HeartbeatLoopbackTests.cs -- confirms State Vector verification
4. Grep for `extractor.Extract` in HeartbeatLoopbackTests.cs -- confirms extraction step
5. Grep for `coordinator.Process` in HeartbeatLoopbackTests.cs -- confirms processing step
6. Grep for `CorrelationId` in HeartbeatLoopbackTests.cs -- confirms correlation propagation check
7. No test produces warnings about skipped or inconclusive tests
</verification>

<success_criteria>
- HeartbeatLoopbackTests: 2 tests proving end-to-end data flow (extraction + processing + state vector + correlation)
- Full test suite passes: `dotnet test` exits with code 0, zero failures
- Total test count verified (60 existing + new unit tests + integration tests)
- Pipeline proved working: varbinds -> extract -> process -> state vector entry with correlationId
</success_criteria>

<output>
After completion, create `.planning/phases/10-end-to-end-integration-testing/10-04-SUMMARY.md`
</output>
