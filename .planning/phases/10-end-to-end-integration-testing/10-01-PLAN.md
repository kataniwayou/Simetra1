---
phase: 10-end-to-end-integration-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Simetra.Tests/Pipeline/TrapFilterTests.cs
  - tests/Simetra.Tests/Pipeline/DeviceRegistryTests.cs
  - tests/Simetra.Tests/Pipeline/CorrelationServiceTests.cs
  - tests/Simetra.Tests/Pipeline/DeviceChannelManagerTests.cs
  - tests/Simetra.Tests/Pipeline/TrapPipelineBuilderTests.cs
  - tests/Simetra.Tests/Models/PollDefinitionDtoTests.cs
autonomous: true

must_haves:
  truths:
    - "TrapFilter.Match returns matching PollDefinitionDto when varbind OIDs intersect, null when they do not"
    - "DeviceRegistry.TryGetDevice finds devices by normalized IPv4 with MapToIPv4, TryGetDeviceByName is case-insensitive, module devices override config on IP collision"
    - "RotatingCorrelationService.CurrentCorrelationId starts empty, SetCorrelationId updates it, and CorrelationIdMiddleware stamps envelope before calling next"
    - "DeviceChannelManager drops oldest items when channel is full, GetWriter/GetReader throw on unknown device, CompleteAll prevents further writes"
    - "TrapPipelineBuilder executes middleware in registration order, terminal delegate is no-op, short-circuit skips downstream middleware"
    - "PollDefinitionDto.FromOptions converts MetricPollOptions correctly with defensive EnumMap copy and ReadOnly Oids"
  artifacts:
    - path: "tests/Simetra.Tests/Pipeline/TrapFilterTests.cs"
      provides: "TrapFilter unit tests"
      contains: "TrapFilterTests"
    - path: "tests/Simetra.Tests/Pipeline/DeviceRegistryTests.cs"
      provides: "DeviceRegistry unit tests"
      contains: "DeviceRegistryTests"
    - path: "tests/Simetra.Tests/Pipeline/CorrelationServiceTests.cs"
      provides: "RotatingCorrelationService and CorrelationIdMiddleware unit tests"
      contains: "CorrelationServiceTests"
    - path: "tests/Simetra.Tests/Pipeline/DeviceChannelManagerTests.cs"
      provides: "DeviceChannelManager backpressure and lifecycle tests"
      contains: "DeviceChannelManagerTests"
    - path: "tests/Simetra.Tests/Pipeline/TrapPipelineBuilderTests.cs"
      provides: "Middleware chain composition tests"
      contains: "TrapPipelineBuilderTests"
    - path: "tests/Simetra.Tests/Models/PollDefinitionDtoTests.cs"
      provides: "PollDefinitionDto.FromOptions tests"
      contains: "PollDefinitionDtoTests"
  key_links:
    - from: "tests/Simetra.Tests/Pipeline/TrapFilterTests.cs"
      to: "TrapFilter"
      via: "Direct instantiation with mock logger"
      pattern: "new TrapFilter"
    - from: "tests/Simetra.Tests/Pipeline/DeviceRegistryTests.cs"
      to: "DeviceRegistry"
      via: "Direct instantiation with Options.Create"
      pattern: "new DeviceRegistry"
    - from: "tests/Simetra.Tests/Pipeline/DeviceChannelManagerTests.cs"
      to: "DeviceChannelManager"
      via: "Direct instantiation with bounded capacity"
      pattern: "new DeviceChannelManager"
---

<objective>
Unit tests for extraction pipeline components: TrapFilter, DeviceRegistry, RotatingCorrelationService + CorrelationIdMiddleware, DeviceChannelManager backpressure, TrapPipelineBuilder middleware composition, and PollDefinitionDto model conversion.

Purpose: Covers TEST-02 (PollDefinitionDto), TEST-03 (device filter + trap filter), TEST-07 (correlation ID), TEST-08 (channel backpressure), TEST-09 (middleware chain). TEST-01 (extractor) is already complete with 15 existing tests.

Output: Six new test files in Pipeline/ and Models/ subdirectories, verifying all pipeline routing, filtering, channel, and middleware logic.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-end-to-end-integration-testing/10-RESEARCH.md
@src/Simetra/Pipeline/TrapFilter.cs
@src/Simetra/Pipeline/DeviceRegistry.cs
@src/Simetra/Pipeline/DeviceChannelManager.cs
@src/Simetra/Pipeline/TrapPipelineBuilder.cs
@src/Simetra/Pipeline/RotatingCorrelationService.cs
@src/Simetra/Pipeline/Middleware/CorrelationIdMiddleware.cs
@src/Simetra/Models/PollDefinitionDto.cs
@src/Simetra/Models/OidEntryDto.cs
@src/Simetra/Models/ExtractionResult.cs
@src/Simetra/Pipeline/TrapEnvelope.cs
@src/Simetra/Pipeline/TrapContext.cs
@src/Simetra/Pipeline/DeviceInfo.cs
@src/Simetra/Configuration/DevicesOptions.cs
@src/Simetra/Configuration/DeviceOptions.cs
@src/Simetra/Configuration/ChannelsOptions.cs
@src/Simetra/Configuration/MetricPollOptions.cs
@src/Simetra/Configuration/OidEntryOptions.cs
@src/Simetra/Configuration/MetricType.cs
@src/Simetra/Configuration/OidRole.cs
@src/Simetra/Configuration/MetricPollSource.cs
@src/Simetra/Pipeline/IDeviceChannelManager.cs
@src/Simetra/Pipeline/ICorrelationService.cs
@src/Simetra/Pipeline/TrapMiddlewareDelegate.cs
@src/Simetra/Pipeline/ITrapMiddleware.cs
@src/Simetra/Devices/IDeviceModule.cs
@tests/Simetra.Tests/Extraction/SnmpExtractorTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: TrapFilter, DeviceRegistry, and PollDefinitionDto tests</name>
  <files>
    tests/Simetra.Tests/Pipeline/TrapFilterTests.cs
    tests/Simetra.Tests/Pipeline/DeviceRegistryTests.cs
    tests/Simetra.Tests/Models/PollDefinitionDtoTests.cs
  </files>
  <action>
    Create the Pipeline/ and Models/ directories under tests/Simetra.Tests/ if they do not exist.

    **TrapFilterTests.cs** (namespace Simetra.Tests.Pipeline):
    - Instantiate real TrapFilter with `Mock.Of&lt;ILogger&lt;TrapFilter&gt;&gt;()`
    - Helper method `MakeDefinition(string metricName, params string[] oids)` that creates PollDefinitionDto with OidEntryDto entries (Role=Metric)
    - Helper method `MakeDevice(string name, params PollDefinitionDto[] defs)` that creates DeviceInfo
    - Tests:
      1. `Match_WhenVarbindOidMatchesDefinition_ReturnsDefinition` -- single OID match returns the definition
      2. `Match_WhenNoOidMatches_ReturnsNull` -- no intersection returns null
      3. `Match_WithMultipleDefinitions_ReturnsFirstMatch` -- first matching definition wins
      4. `Match_WithMultipleVarbinds_MatchesAny` -- any varbind matching any OID in definition triggers match
      5. `Match_EmptyVarbinds_ReturnsNull` -- empty varbind list returns null
    - Use `new Variable(new ObjectIdentifier("1.3.6.1.2.1.1.0"), new Integer32(42))` for test varbinds
    - Import Lextm.SharpSnmpLib for Variable, ObjectIdentifier, Integer32

    **DeviceRegistryTests.cs** (namespace Simetra.Tests.Pipeline):
    - Instantiate real DeviceRegistry with `Options.Create(devicesOptions)` and mock modules
    - Helper method to build DevicesOptions with one or more DeviceOptions
    - Tests:
      1. `TryGetDevice_KnownIp_ReturnsTrue` -- parse IP, find device
      2. `TryGetDevice_UnknownIp_ReturnsFalse` -- non-existent IP returns false
      3. `TryGetDevice_NormalizesIpv6MappedToIpv4` -- IPv6-mapped (::ffff:10.0.0.1) resolves same as 10.0.0.1
      4. `TryGetDeviceByName_CaseInsensitive` -- "Router-1" matches "router-1"
      5. `TryGetDeviceByName_UnknownName_ReturnsFalse` -- non-existent name returns false
      6. `ModuleDevices_OverrideConfigOnIpCollision` -- register config device at 10.0.0.1, then module device at 10.0.0.1, verify TryGetDevice returns module device
    - For module tests: create `Mock&lt;IDeviceModule&gt;` with DeviceName, IpAddress, DeviceType, TrapDefinitions

    **PollDefinitionDtoTests.cs** (namespace Simetra.Tests.Models):
    - Tests:
      1. `FromOptions_PreservesMetricNameAndMetricType` -- MetricName and MetricType propagated
      2. `FromOptions_ConvertsOidEntriesToDto` -- OidEntryOptions become OidEntryDto with correct Role, PropertyName, Oid
      3. `FromOptions_CreatesDefensiveEnumMapCopy` -- modify original dictionary after FromOptions, verify DTO copy is unaffected
      4. `FromOptions_PreservesSourceField` -- Source=Configuration and Source=Module both propagated
      5. `FromOptions_OidsListIsReadOnly` -- verify returned Oids is IReadOnlyList (attempting Add on underlying list throws)
    - Create MetricPollOptions instances with populated fields for each test
  </action>
  <verify>
    `dotnet test tests/Simetra.Tests/ --filter "FullyQualifiedName~TrapFilterTests|FullyQualifiedName~DeviceRegistryTests|FullyQualifiedName~PollDefinitionDtoTests"` -- all tests pass.
  </verify>
  <done>
    TrapFilter tested for OID matching/non-matching/multi-definition/multi-varbind/empty cases. DeviceRegistry tested for IP lookup, IPv4 normalization, case-insensitive name lookup, module override on collision. PollDefinitionDto tested for field preservation, OID conversion, defensive EnumMap copy, Source propagation, ReadOnly Oids. Covers TEST-02, TEST-03.
  </done>
</task>

<task type="auto">
  <name>Task 2: CorrelationService, DeviceChannelManager, and TrapPipelineBuilder tests</name>
  <files>
    tests/Simetra.Tests/Pipeline/CorrelationServiceTests.cs
    tests/Simetra.Tests/Pipeline/DeviceChannelManagerTests.cs
    tests/Simetra.Tests/Pipeline/TrapPipelineBuilderTests.cs
  </files>
  <action>
    **CorrelationServiceTests.cs** (namespace Simetra.Tests.Pipeline):
    - Test RotatingCorrelationService (direct instantiation, no mocks):
      1. `CurrentCorrelationId_InitiallyEmpty` -- new instance returns string.Empty
      2. `SetCorrelationId_UpdatesCurrentValue` -- set "abc-123", read back "abc-123"
      3. `SetCorrelationId_OverwritesPreviousValue` -- set twice, second value wins
    - Test CorrelationIdMiddleware (direct instantiation with real CorrelationService):
      4. `InvokeAsync_StampsCorrelationIdOnEnvelope` -- set correlationId on service, invoke middleware, verify envelope.CorrelationId matches
      5. `InvokeAsync_CallsNextDelegate` -- verify next delegate is invoked after stamping
    - For middleware tests: create real RotatingCorrelationService, set a known correlationId, create CorrelationIdMiddleware with it, invoke with a TrapContext containing a TrapEnvelope, verify envelope.CorrelationId is updated and next was called

    **DeviceChannelManagerTests.cs** (namespace Simetra.Tests.Pipeline):
    - Instantiate real DeviceChannelManager with small BoundedCapacity (2) for backpressure testing
    - Helper `MakeDevicesOptions(params string[] deviceNames)` that creates DevicesOptions with DeviceOptions entries
    - Helper `MakeEnvelope(string correlationId)` that creates TrapEnvelope with loopback IP and empty varbinds
    - Tests:
      1. `GetWriter_KnownDevice_ReturnsWriter` -- GetWriter does not throw for registered device
      2. `GetReader_KnownDevice_ReturnsReader` -- GetReader does not throw for registered device
      3. `GetWriter_UnknownDevice_ThrowsKeyNotFound` -- unregistered device name throws KeyNotFoundException
      4. `GetReader_UnknownDevice_ThrowsKeyNotFound` -- same for reader
      5. `DeviceNames_ReturnsAllRegistered` -- DeviceNames collection contains all configured device names
      6. `WriteToFullChannel_DropsOldestItem` -- fill channel to capacity (2 items), write a 3rd, read 2 items and verify they are items 2 and 3 (item 1 was dropped)
      7. `CompleteAll_PreventsSubsequentWrites` -- CompleteAll, then WriteAsync throws ChannelClosedException
      8. `ModuleDevices_CreateChannels` -- register via IDeviceModule mock, verify channel exists for module device name
    - For DropOldest test (TEST-08 key behavior): write env1, env2 to capacity-2 channel. Write env3. Read two items. First read should be env2 (not env1 which was dropped). Second read should be env3.
    - Use Moq for IDeviceModule in module channel test

    **TrapPipelineBuilderTests.cs** (namespace Simetra.Tests.Pipeline):
    - Test TrapPipelineBuilder (direct instantiation, no mocks):
      1. `Build_WithNoMiddleware_ReturnsNoOpDelegate` -- build empty pipeline, invoke with context, no exception
      2. `Build_MiddlewareExecutesInRegistrationOrder` -- register 3 middleware that each add to a shared List&lt;int&gt;, invoke, verify list is [1, 2, 3]
      3. `Build_MiddlewareCanShortCircuit` -- register middleware that does NOT call next, verify subsequent middleware is NOT executed
      4. `Use_ITrapMiddleware_Overload_Works` -- register middleware via ITrapMiddleware interface overload, verify it executes
      5. `Build_TerminalDelegateIsNoOp` -- last middleware calls next (terminal), verify no exception (terminal is Task.CompletedTask)
    - For TrapContext in tests: create TrapEnvelope with `required` properties (Varbinds as empty list, SenderAddress as IPAddress.Loopback, ReceivedAt as DateTimeOffset.UtcNow), wrap in TrapContext
    - For ITrapMiddleware overload test: create a simple ITrapMiddleware implementation (anonymous via Moq or small test class) that sets a flag
  </action>
  <verify>
    `dotnet test tests/Simetra.Tests/ --filter "FullyQualifiedName~CorrelationServiceTests|FullyQualifiedName~DeviceChannelManagerTests|FullyQualifiedName~TrapPipelineBuilderTests"` -- all tests pass.
  </verify>
  <done>
    CorrelationService tested for initial empty state, set/overwrite, and middleware stamping behavior. DeviceChannelManager tested for writer/reader access, unknown device errors, DropOldest backpressure, CompleteAll lifecycle, and module device channels. TrapPipelineBuilder tested for empty pipeline, execution order, short-circuit, ITrapMiddleware overload, and terminal no-op. Covers TEST-07, TEST-08, TEST-09.
  </done>
</task>

</tasks>

<verification>
1. `dotnet test tests/Simetra.Tests/` -- all tests pass (existing 60 + new tests)
2. `dotnet test tests/Simetra.Tests/ --filter "FullyQualifiedName~Pipeline"` -- all pipeline tests pass
3. `dotnet test tests/Simetra.Tests/ --filter "FullyQualifiedName~Models"` -- PollDefinitionDto tests pass
4. Grep for `class TrapFilterTests` in tests/ -- found in Pipeline/
5. Grep for `class DeviceRegistryTests` in tests/ -- found in Pipeline/
6. Grep for `class DeviceChannelManagerTests` in tests/ -- found in Pipeline/
7. Grep for `DropsOldestItem` in tests/ -- confirms DropOldest backpressure test exists (TEST-08)
8. Grep for `RegistrationOrder` in tests/ -- confirms middleware ordering test exists (TEST-09)
</verification>

<success_criteria>
- All new tests pass alongside existing 60 tests (no regressions)
- TrapFilter: 5 tests covering match/non-match/multi-def/multi-varbind/empty
- DeviceRegistry: 6 tests covering IP lookup, IPv4 normalization, name case-insensitivity, module override
- PollDefinitionDto: 5 tests covering field preservation, OID conversion, defensive copy, Source, ReadOnly
- CorrelationService: 5 tests covering initial empty, set/overwrite, middleware stamp, next invocation
- DeviceChannelManager: 8 tests covering access, errors, DropOldest, CompleteAll, module devices
- TrapPipelineBuilder: 5 tests covering empty, order, short-circuit, ITrapMiddleware, terminal
- TEST-01 confirmed passing (existing 15 extractor tests)
</success_criteria>

<output>
After completion, create `.planning/phases/10-end-to-end-integration-testing/10-01-SUMMARY.md`
</output>
