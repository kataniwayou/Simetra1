---
phase: 02-domain-models-extraction-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Simetra/Simetra.csproj
  - src/Simetra/Models/OidEntryDto.cs
  - src/Simetra/Models/PollDefinitionDto.cs
  - src/Simetra/Models/ExtractionResult.cs
  - src/Simetra/Pipeline/ISnmpExtractor.cs
autonomous: true

must_haves:
  truths:
    - "PollDefinitionDto is an immutable record with MetricName, MetricType, Oids (IReadOnlyList<OidEntryDto>), IntervalSeconds, and Source fields"
    - "OidEntryDto is an immutable record with Oid, PropertyName, Role, and optional EnumMap (IReadOnlyDictionary<int, string>?)"
    - "ExtractionResult holds Metrics (Dictionary<string, long>), Labels (Dictionary<string, string>), EnumMapMetadata, and a reference to the PollDefinitionDto that produced it"
    - "PollDefinitionDto.FromOptions(MetricPollOptions) converts mutable config Options to immutable runtime DTO preserving Source field"
    - "ISnmpExtractor interface defines Extract(IList<Variable>, PollDefinitionDto) returning ExtractionResult"
    - "SharpSnmpLib 12.5.7 is added as a package reference to Simetra.csproj"
  artifacts:
    - path: "src/Simetra/Models/OidEntryDto.cs"
      provides: "Immutable OID entry runtime DTO"
      contains: "sealed record OidEntryDto"
    - path: "src/Simetra/Models/PollDefinitionDto.cs"
      provides: "Immutable poll definition runtime DTO with FromOptions factory"
      contains: "sealed record PollDefinitionDto"
    - path: "src/Simetra/Models/ExtractionResult.cs"
      provides: "Extraction output container with metrics, labels, and enum metadata"
      contains: "sealed class ExtractionResult"
    - path: "src/Simetra/Pipeline/ISnmpExtractor.cs"
      provides: "Extractor contract for pipeline"
      contains: "interface ISnmpExtractor"
  key_links:
    - from: "src/Simetra/Models/PollDefinitionDto.cs"
      to: "src/Simetra/Configuration/MetricPollOptions.cs"
      via: "FromOptions static factory method"
      pattern: "FromOptions.*MetricPollOptions"
    - from: "src/Simetra/Models/PollDefinitionDto.cs"
      to: "src/Simetra/Models/OidEntryDto.cs"
      via: "Oids property typed as IReadOnlyList<OidEntryDto>"
      pattern: "IReadOnlyList<OidEntryDto>"
    - from: "src/Simetra/Pipeline/ISnmpExtractor.cs"
      to: "src/Simetra/Models/ExtractionResult.cs"
      via: "Extract method return type"
      pattern: "ExtractionResult Extract"
---

<objective>
Create the immutable runtime domain models (PollDefinitionDto, OidEntryDto, ExtractionResult) and the ISnmpExtractor interface that form the contract between configuration (Phase 1) and the extraction engine.

Purpose: These models are the bridge between the mutable configuration Options (Phase 1) and the immutable pipeline runtime. Every downstream phase (3-10) depends on PollDefinitionDto as the unified structure for trap definitions, state polls, and metric polls.

Output: Four source files in Models/ and Pipeline/, plus SharpSnmpLib package reference.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-domain-models-extraction-engine/02-RESEARCH.md
@src/Simetra/Configuration/MetricPollOptions.cs
@src/Simetra/Configuration/OidEntryOptions.cs
@src/Simetra/Configuration/MetricType.cs
@src/Simetra/Configuration/OidRole.cs
@src/Simetra/Configuration/MetricPollSource.cs
@src/Simetra/Simetra.csproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SharpSnmpLib package and create domain model records</name>
  <files>
    src/Simetra/Simetra.csproj
    src/Simetra/Models/OidEntryDto.cs
    src/Simetra/Models/PollDefinitionDto.cs
    src/Simetra/Models/ExtractionResult.cs
  </files>
  <action>
    1. Add SharpSnmpLib 12.5.7 package reference to Simetra.csproj:
       ```xml
       <PackageReference Include="Lextm.SharpSnmpLib" Version="12.5.7" />
       ```

    2. Create `src/Simetra/Models/OidEntryDto.cs`:
       - Namespace: `Simetra.Models`
       - `public sealed record OidEntryDto(string Oid, string PropertyName, OidRole Role, IReadOnlyDictionary<int, string>? EnumMap)`
       - Using `Simetra.Configuration` for OidRole enum
       - XML doc comments on the record and each parameter

    3. Create `src/Simetra/Models/PollDefinitionDto.cs`:
       - Namespace: `Simetra.Models`
       - `public sealed record PollDefinitionDto(string MetricName, MetricType MetricType, IReadOnlyList<OidEntryDto> Oids, int IntervalSeconds, MetricPollSource Source)`
       - Using `Simetra.Configuration` for MetricType and MetricPollSource enums
       - Static factory method `public static PollDefinitionDto FromOptions(MetricPollOptions options)` that:
         - Maps each `OidEntryOptions` to `OidEntryDto` (copying EnumMap via `o.EnumMap?.ToDictionary(kv => kv.Key, kv => kv.Value).AsReadOnly()` to create a new IReadOnlyDictionary, or null if source is null)
         - Wraps the OidEntryDto list with `.ToList().AsReadOnly()` for IReadOnlyList
         - Preserves the Source field from MetricPollOptions (already stamped by PostConfigure in Phase 1)
       - XML doc comments on the record, parameters, and the factory method

    4. Create `src/Simetra/Models/ExtractionResult.cs`:
       - Namespace: `Simetra.Models`
       - `public sealed class ExtractionResult` (class, not record -- mutable construction via init properties)
       - Properties with `{ get; init; }`:
         - `PollDefinitionDto Definition` (required, `= null!` default)
         - `IReadOnlyDictionary<string, long> Metrics` (default: empty dictionary)
         - `IReadOnlyDictionary<string, string> Labels` (default: empty dictionary)
         - `IReadOnlyDictionary<string, IReadOnlyDictionary<int, string>> EnumMapMetadata` (default: empty dictionary)
       - XML doc comments explaining:
         - Metrics: raw SNMP numeric values keyed by PropertyName, EnumMap NOT applied
         - Labels: enum-mapped strings or raw strings keyed by PropertyName
         - EnumMapMetadata: stored for Grafana value mappings, NOT used as metric values
  </action>
  <verify>
    Run `dotnet build src/Simetra/Simetra.csproj` -- must compile with zero errors and zero warnings.
    Verify SharpSnmpLib restores: `dotnet restore src/Simetra/Simetra.csproj` succeeds.
  </verify>
  <done>
    - OidEntryDto is a sealed record in Simetra.Models with Oid, PropertyName, Role, EnumMap properties
    - PollDefinitionDto is a sealed record in Simetra.Models with MetricName, MetricType, Oids, IntervalSeconds, Source properties
    - PollDefinitionDto.FromOptions(MetricPollOptions) converts mutable Options to immutable DTO
    - ExtractionResult has Metrics (long), Labels (string), EnumMapMetadata, and Definition properties
    - SharpSnmpLib 12.5.7 is a package reference in Simetra.csproj
    - Project compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ISnmpExtractor interface</name>
  <files>
    src/Simetra/Pipeline/ISnmpExtractor.cs
  </files>
  <action>
    Create `src/Simetra/Pipeline/ISnmpExtractor.cs`:
    - Namespace: `Simetra.Pipeline`
    - Using `Lextm.SharpSnmpLib` for Variable type
    - Using `Simetra.Models` for PollDefinitionDto and ExtractionResult
    - `public interface ISnmpExtractor` with single method:
      ```csharp
      ExtractionResult Extract(IList<Variable> varbinds, PollDefinitionDto definition);
      ```
    - XML doc comments on the interface and method:
      - Interface: "Extracts SNMP varbind data into an ExtractionResult using PollDefinitionDto instructions. Same logic for traps and polls -- no per-device-type logic."
      - Method: parameter docs for varbinds ("SNMP varbinds from trap or poll response") and definition ("Poll definition with OID entries and roles"), return doc ("Extraction result with metrics, labels, and enum-map metadata")
  </action>
  <verify>
    Run `dotnet build src/Simetra/Simetra.csproj` -- must compile with zero errors.
    Verify the Pipeline directory contains ISnmpExtractor.cs.
  </verify>
  <done>
    - ISnmpExtractor interface exists in Simetra.Pipeline namespace
    - Extract method accepts IList<Variable> and PollDefinitionDto, returns ExtractionResult
    - Interface uses SharpSnmpLib Variable type (proving package reference works)
    - Project compiles cleanly
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/Simetra/Simetra.csproj` compiles with zero errors
2. `dotnet test tests/Simetra.Tests/Simetra.Tests.csproj` -- all existing 45 Phase 1 tests still pass (no regressions)
3. Files exist: Models/OidEntryDto.cs, Models/PollDefinitionDto.cs, Models/ExtractionResult.cs, Pipeline/ISnmpExtractor.cs
4. PollDefinitionDto.FromOptions is a static method accepting MetricPollOptions
5. All collection properties use IReadOnly* interfaces (IReadOnlyList, IReadOnlyDictionary)
</verification>

<success_criteria>
- Domain model records compile and are usable from test project
- FromOptions factory correctly maps all fields from MetricPollOptions/OidEntryOptions to PollDefinitionDto/OidEntryDto
- ExtractionResult provides typed containers for metrics (long), labels (string), and enum metadata
- ISnmpExtractor interface references SharpSnmpLib Variable type
- All 45 Phase 1 tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-domain-models-extraction-engine/02-01-SUMMARY.md`
</output>
